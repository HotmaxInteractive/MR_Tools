using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Menu_Calibration : MonoBehaviour {

    /* --------------------------
    STATE SETTERS
    1. Set Calibration Mode to Off (close button)
    2. Set Calibration stage (forward/backward button), close button resets stage to 1
    3. Save Calibration (save button)

    STATE GETTERS
    1. Calibration stage (updates Title + Body, shows or hides Save Button, shows or hides Next/Prev/Close)
    -------------------------------*/

    void Start()
    {

    }

    void Update()
    {

    }
}
